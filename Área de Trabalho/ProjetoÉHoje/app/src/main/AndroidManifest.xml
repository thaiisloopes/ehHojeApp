<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.edu.iff.pooa20152.projetohoje">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <meta-data
        android:name="com.google.android.gms.version"
        android:value="@integer/google_play_services_version" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainClienteActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CadastroActivity"
            android:label="Cadastro" />
        <activity
            android:name=".AvaliacaoActivity"
            android:label="Avaliação" />
        <activity
            android:name=".TelaInicialClienteActivity"
            android:label="Cliente - Principal" />
        <activity
            android:name=".SelecionarLocalidadeActivity"
            android:label="Selecionar Localidade" />
        <activity
            android:name=".ContatoActivity"
            android:label="Entre em contato" />
        <activity
            android:name=".CadastrarEventosActivity"
            android:label="Cadastrar Eventos" />
        <activity
            android:name=".TelaInicialEstabelecimentoActivity"
            android:label="Estabelecimento - Principal" />
        <activity
            android:name=".CadastrarRepresentantesActivity"
            android:label="Cadastrar Representantes" />
        <activity
            android:name=".ComprarIngressoActivity"
            android:label="Comprar Ingresso" />
        <activity
            android:name=".RemoverRepresentantesActivity"
            android:label="Remover Representante" />
        <activity
            android:name=".MainEstabelecimentoActivity"
            android:label="Login Estabelecimento" />
        <activity
            android:name=".InformacoesEvento01Activity"
            android:label="Informações Evento" />
        <activity
            android:name=".CadastrarEstabelecimentoActivity"
            android:label="Cadastrar Estabelecimento"/>
    </application>

</manifest>